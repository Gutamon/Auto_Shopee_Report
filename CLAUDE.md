# 蝦皮自動化報表工具

### 請用繁體中文

## 專案目標

匯入蝦皮賣家後台匯出的「原始資料.xlsx」（整月訂單明細），一次輸出「週報」與「月報」。
每份報表用一個工作頁列出：
- 所有非「不成立」的訂單資料
- 期間訂單金額與完整成本結構（手續費、服務費、金流費、退貨、運費、發票費、廣告費、實際進帳）
- 各品項的出貨數量與銷售金額
- 銷售數量排行圖表、銷售金額排行圖表

**技術路線**：免部署的單一 HTML 工具。使用者拖拽/選取「原始資料.xlsx」進頁面，瀏覽器端用 JavaScript（SheetJS 等）本機解析與運算，不上傳任何資料到伺服器。工具需能一次選取期間、輸出週報與月報兩份結果。

> 本文件於 2026-09-11 依據 `範例/週報.xlsx`、`範例/月報.xlsx`、`範例/原始資料.xlsx` 三份範例檔案逆向拆解 Excel 公式與資料結構後，與使用者逐項確認規則而寫成。2026-09-17 進一步逆向拆解「訂單退貨」「發票費用」「廣告費用」欄位的實際 Excel 公式並與使用者確認，更新如下。**規則有變動或發現新例外時，請務必同步更新本文件**，不要只改程式不改文件。

## 原始資料結構（`原始資料.xlsx`, sheet: `orders`）

一列 = 一筆「訂單內的一個商品選項」（同一訂單編號可能重複多列，各列是不同商品/規格）。57 欄，關鍵欄位（0-index）：

| index | 欄名 | 用途 |
|---|---|---|
| 0 | 訂單編號 | 同一訂單多列時共用此編號 |
| 1 | 訂單狀態 | 過濾依據，見下方「核心過濾規則」 |
| 3 | 不成立原因 | 過濾例外判斷依據 |
| 6 | 訂單成立日期 | 週期篨選依據 |
| 19 | 成交手續費 | 成本結構用 |
| 20 | 其他服務費 | 成本結構用 |
| 21 | 金流與系統處理費 | 成本結構用 |
| 27 | 商品選項名稱 | 品項分組鍵（如「天海1公斤」）|
| 31 | 商品活動價格 | 實際成交單價 |
| 33 | 商品選項貨號 | 品項分組鍵（可用來代替 27，兩者對應同一品項）|
| 34 | 數量 | 出貨數量 |
| 35 | 退貨數量 | 目前範例未使用，暫不處理 |

「總價」= 商品活動價格 × 數量（範例中用公式 `=H*I`，對應到程式邏輯即為每列的小計）。

**重要：訂單層級費用會在同訂單的每一列重複出現，加總時每張訂單只能算一次。** 同一「訂單編號」若因多個商品選項拆成多列，原始資料的「成交手續費」「其他服務費」「金流與系統處理費」三欄會在該訂單的每一列填入**完全相同的值**（訂單層級費用，不是逐商品攤分），而不是像「總價」那樣逐列各自不同。因此：
- 「總價」（商品活動價格 × 數量）：**逐列**加總，因為每列本來就是不同商品/金額。
- 「成交手續費」「其他服務費」「金流與系統處理費」：**每個訂單編號只能加總一次**（取該訂單第一次出現的值即可），逐列加總會依商品選項數重複計入、大幅高估成本。
- 此規則同時套用在「期間蝦皮訂單金額」旁的三項手續費，以及「訂單退貨」計算式裡「尚未完成訂單」對應的手續費扣除（也必須先按訂單去重再加總，否則訂單退貨的預扣金額也會被高估）。
- 2026-09-17 使用者回報週報範例的「成交手續費／其他服務費／金流處理費」三項跟工具算出來有些許落差，逐筆比對後確認**不是**去重邏輯錯誤：原因與下方「訂單退貨」段落相同的「範例報表與原始資料非同一時間點快照」——有少數訂單（約 12 筆）在範例報表產生當下手續費三欄有實際數值，但目前這份原始資料裡同一批訂單的手續費三欄已被蝦皮後台歸零（研判是不成立/例外類訂單在報表產生「之後」才被系統回頭處理，把手續費欄位清成 0）。這些訂單的差額加總後正好等於觀察到的落差金額。**結論：工具的「每訂單只取第一次出現值」去重演算法本身是對的，此類落差屬於資料快照時間差，非程式邏輯問題**，之後若使用者再回報類似「三項手續費對不起來」，優先檢查是否為同一成因，而非重新檢視去重程式碼。

## 核心過濾規則（重要，非直覺）

**保留條件**：`訂單狀態 ≠ 不成立`，**但有例外**——若「不成立原因」屬於下列「賣家已出貨、非買家/賣家主動造成」的系統判定類型，仍要保留計入報表：
- 「系統自動判定訂單不成立，原因:無法順利配送」
- 「系統自動判定訂單不成立，原因:包裹驗收失敗」

**排除**（即使狀態不是「不成立」文字本身，這條只是重申「不成立」的一般情況要被排除）：
- 被買家取消（改變心意、更換優惠券、修改訂單內容、更改地址等所有「被買家取消」原因）
- 系統判定「未收到款項」
- 賣家選擇不成立（如無法運送範圍）
- 系統判定「物流商已通知包裹遺失」的不成立（此為蝦皮賠款範疇，非賣家出貨完成，維持排除）

> 此例外規則已用月報範例中實際 6 筆「不成立但仍被列入報表」的訂單向使用者逐筆核實原因後確認為真規則，非人工製表疏漏。若未來遇到新的「不成立原因」文字，先判斷它是否屬於「賣家已出貨、只是後續配送/驗收出問題」，不要自行擴大或縮小例外範圍，應詢問使用者確認。

**訂單完成度分類**（影響成本結構「訂單退貨」項）：
- 「運送中」「待出貨」視為**尚未完成**的訂單。這些訂單仍計入品項出貨數量/銷售金額與訂單金額（商品已賣出、款項邏輯上已產生），但因蝦皮尚未撥款，需在成本結構的「訂單退貨」項中，把這些訂單的金額扣除「其對應的手續費/服務費/金流費」後，從「期間蝦皮進帳」中預先扣掉（因為這些費用尚未真正發生）。
- 2026-09-17 逆向拆解範例公式時發現：`範例/月報.xlsx`、`範例/週報.xlsx` 裡「訂單退貨」實際引用的是工作表最下方另外手動貼上的一小塊訂單清單（與主表「運送中/待出貨」狀態的訂單完全沒有重疊，甚至包含「已完成」狀態、日期是前一個月的舊訂單）。經與使用者確認，這是因為範例檔案是業主在不同時間點分次提供、原始資料與範例報表的時間軸沒有完全對齊所致（例如做週報當下抓的原始資料比較早，之後某些「運送中」訂單才確定不成立），**不是**代表要改成手動整理清單。因此本工具維持用「運送中/待出貨」狀態自動判定「訂單退貨」，不需要額外的手動勾選介面。

## 週期篨選

以「訂單成立日期」欄位篨選出週報／月報各自的日期區間。原始資料是整月匯入一次，週報/月報都是從同一份月資料中依區間篩選產生，**不需要使用者針對週報/月報分別匯出資料**。

週報與月報本質是同一套邏輯、同一份原始資料，唯一差異是篩選區間長度（週報約 7-10 天、月報約 1 個月）與報表標題文字，不需要兩套計算邏輯。

## 報表輸出內容規格

### 1. 訂單明細（非不成立列表）
依上述過濾規則列出當期所有保留列。

### 2. 訂單金額成本結構

| 項目 | 算法 | 是否可自動算 |
|---|---|---|
| 期間蝦皮訂單金額 | 篨選後（含例外保留）列的「總價」加總 | 可自動算 |
| 成交手續費 | 篨選後列「成交手續費」加總 | 可自動算 |
| 其他服務費 | 篨選後列「其他服務費」加總 | 可自動算 |
| 金流處理費 | 篨選後列「金流與系統處理費」加總 | 可自動算 |
| 訂單退貨 | 「運送中/待出貨」訂單的總價加總，再扣除這些訂單的手續費+服務費+金流費 | 可自動算 |
| 發票費用 | 不重複訂單編號數 × 2.5 元（範例公式 `=SUMPRODUCT(1/COUNTIF(A:A,A:A))*2.5`，兩者等價）| 可自動算 |
| 大榮＋補寄運費 | 原始資料沒有此欄，是賣家額外實付物流費 | **無法自動算**，工具需提供「逐筆訂單」手動輸入框，預設 0 |
| 廣告扣款 | 蝦皮廣告後台可匯出「日期＋每日金額」明細表；期間廣告費用＝依訂單成立日期同一區間，加總明細表中落在該區間的每日金額 | 若使用者有匯入廣告明細檔可自動算（依日期區間篩選加總），否則退回手動輸入單一總額，預設 0 |
| 期間蝦皮進帳 | 訂單金額 − 成交手續費 − 其他服務費 − 金流處理費 − 訂單退貨 − 大榮運費 − 發票費用 − 廣告扣款 | 自動算（依上列各項）|

### 3. 品項出貨數量與銷售金額

- 依「商品選項貨號」分組，統計當期出貨數量（數量加總）與銷售金額（總價加總）。**分組鍵與表格/圖表上顯示的品項名稱都用「商品選項貨號」**（若貨號欄位為空才退而用「商品選項名稱」補位），不使用「商品選項名稱」當顯示名稱。
- **不維護品項主檔**：售價採當期實際成交價，只列出當期實際出現過的品項；不需列出本期未出貨的品項（與人工範例的作法不同，範例會列所有固定品項含0，本工具簡化為僅列本期實際出現者）。

### 4. 排行圖表

- 從當期出現的品項中，各自依「銷售數量」與「銷售金額」排序取前 10 名。
- 產出兩張圖：數量排行圖、金額排行圖。

## 廣告費用明細檔（選填匯入，第二個檔案）

2026-09-17 逆向拆解範例報表時發現：報表上寫死的「廣告扣款」數字（如 28702）其實等於蝦皮廣告後台可匯出的「日期＋每日金額」明細表加總後四捨五入的結果（範例中為 -28701.59 → 28702）。因此工具提供**第二個、選填的檔案上傳欄位**，讓使用者直接拖入蝦皮廣告後台匯出的 Excel/CSV：

- 欄位辨識採**寬鬆比對表頭名稱**（因週報/月報範例中「類型」欄文字不一致，如「蝦皮廣告」vs「商品廣告 」含尾隨空白，不可用來判斷，只用「日期」「金額」兩欄）：
  - 日期欄：表頭包含「日期」的欄位
  - 金額欄：表頭包含「金額」的欄位（金額為負數或正數皆可，一律取絕對值加總）
- 週報／月報各自依「訂單成立日期」相同的起訖區間，篩選明細表中日期落在區間內的列，加總其金額欄絕對值，作為該期間的「廣告扣款」。
- 若使用者沒有上傳此明細檔，退回單一手動輸入總額（見下）。
- 若使用者兩者都填（既上傳明細檔、又手動輸入總額），以明細檔篩選加總的自動結果為準，手動總額視為未提供。

## 已知需手動輸入的資料（無法從原始資料取得）

1. **大榮＋補寄運費**：逐筆訂單金額，工具需提供輸入介面。目前無範例資料來源可整合，維持手動輸入；未來若使用者能提供大榮對帳單匯出格式，可再擴充自動抓取。
2. **廣告扣款**：優先使用上述「廣告費用明細檔」自動計算；若使用者未匯入明細檔，才使用單一期間總額的手動輸入欄位（預設 0）。

## 待確認/未來可能擴充方向

- 若日後「不成立原因」出現新的文字類型，需要重新跟使用者確認是否納入保留例外。
- 「退貨數量」欄位（原始資料第 35 欄）目前範例未使用，暫不處理，若使用者提出退貨相關報表需求再另行討論。
- 大榮運費若未來有可匯入的外部資料格式（如大榮對帳單），需比照廣告費用明細檔的做法重新設計匯入介面。
