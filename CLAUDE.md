# 蝦皮自動化報表工具

## 專案目標

匯入蝦皮賣家後台匯出的「原始資料.xlsx」（整月訂單明細），一次輸出「週報」與「月報」。
每份報表用一個工作頁列出：
- 所有非「不成立」的訂單資料
- 期間訂單金額與完整成本結構（手續費、服務費、金流費、退貨、運費、發票費、廣告費、實際進帳）
- 各品項的出貨數量與銷售金額
- 銷售數量排行圖表、銷售金額排行圖表

**技術路線**：免部署的單一 HTML 工具。使用者拖拽/選取「原始資料.xlsx」進頁面，瀏覽器端用 JavaScript（SheetJS 等）本機解析與運算，不上傳任何資料到伺服器。工具需能一次選取期間、輸出週報與月報兩份結果。

> 本文件於 2026-09-11 依據 `範例/週報.xlsx`、`範例/月報.xlsx`、`範例/原始資料.xlsx` 三份範例檔案逆向拆解 Excel 公式與資料結構後，與使用者逐項確認規則而寫成。**規則有變動或發現新例外時，請務必同步更新本文件**，不要只改程式不改文件。

## 原始資料結構（`原始資料.xlsx`, sheet: `orders`）

一列 = 一筆「訂單內的一個商品選項」（同一訂單編號可能重複多列，各列是不同商品/規格）。57 欄，關鍵欄位（0-index）：

| index | 欄名 | 用途 |
|---|---|---|
| 0 | 訂單編號 | 同一訂單多列時共用此編號 |
| 1 | 訂單狀態 | 過濾依據，見下方「核心過濾規則」 |
| 3 | 不成立原因 | 過濾例外判斷依據 |
| 6 | 訂單成立日期 | 週期篨選依據 |
| 19 | 成交手續費 | 成本結構用 |
| 20 | 其他服務費 | 成本結構用 |
| 21 | 金流與系統處理費 | 成本結構用 |
| 27 | 商品選項名稱 | 品項分組鍵（如「天海1公斤」）|
| 31 | 商品活動價格 | 實際成交單價 |
| 33 | 商品選項貨號 | 品項分組鍵（可用來代替 27，兩者對應同一品項）|
| 34 | 數量 | 出貨數量 |
| 35 | 退貨數量 | 目前範例未使用，暫不處理 |

「總價」= 商品活動價格 × 數量（範例中用公式 `=H*I`，對應到程式邏輯即為每列的小計）。

## 核心過濾規則（重要，非直覺）

**保留條件**：`訂單狀態 ≠ 不成立`，**但有例外**——若「不成立原因」屬於下列「賣家已出貨、非買家/賣家主動造成」的系統判定類型，仍要保留計入報表：
- 「系統自動判定訂單不成立，原因:無法順利配送」
- 「系統自動判定訂單不成立，原因:包裹驗收失敗」

**排除**（即使狀態不是「不成立」文字本身，這條只是重申「不成立」的一般情況要被排除）：
- 被買家取消（改變心意、更換優惠券、修改訂單內容、更改地址等所有「被買家取消」原因）
- 系統判定「未收到款項」
- 賣家選擇不成立（如無法運送範圍）
- 系統判定「物流商已通知包裹遺失」的不成立（此為蝦皮賠款範疇，非賣家出貨完成，維持排除）

> 此例外規則已用月報範例中實際 6 筆「不成立但仍被列入報表」的訂單向使用者逐筆核實原因後確認為真規則，非人工製表疏漏。若未來遇到新的「不成立原因」文字，先判斷它是否屬於「賣家已出貨、只是後續配送/驗收出問題」，不要自行擴大或縮小例外範圍，應詢問使用者確認。

**訂單完成度分類**（影響成本結構「訂單退貨」項）：
- 「運送中」「待出貨」視為**尚未完成**的訂單。這些訂單仍計入品項出貨數量/銷售金額與訂單金額（商品已賣出、款項邏輯上已產生），但因蝦皮尚未撥款，需在成本結構的「訂單退貨」項中，把這些訂單的金額扣除「其對應的手續費/服務費/金流費」後，從「期間蝦皮進帳」中預先扣掉（因為這些費用尚未真正發生）。

## 週期篨選

以「訂單成立日期」欄位篨選出週報／月報各自的日期區間。原始資料是整月匯入一次，週報/月報都是從同一份月資料中依區間篩選產生，**不需要使用者針對週報/月報分別匯出資料**。

週報與月報本質是同一套邏輯、同一份原始資料，唯一差異是篩選區間長度（週報約 7-10 天、月報約 1 個月）與報表標題文字，不需要兩套計算邏輯。

## 報表輸出內容規格

### 1. 訂單明細（非不成立列表）
依上述過濾規則列出當期所有保留列。

### 2. 訂單金額成本結構

| 項目 | 算法 | 是否可自動算 |
|---|---|---|
| 期間蝦皮訂單金額 | 篨選後（含例外保留）列的「總價」加總 | 可自動算 |
| 成交手續費 | 篨選後列「成交手續費」加總 | 可自動算 |
| 其他服務費 | 篨選後列「其他服務費」加總 | 可自動算 |
| 金流處理費 | 篨選後列「金流與系統處理費」加總 | 可自動算 |
| 訂單退貨 | 「運送中/待出貨」訂單的總價加總，再扣除這些訂單的手續費+服務費+金流費 | 可自動算 |
| 發票費用 | 不重複訂單編號數 × 2.5 元 | 可自動算 |
| 大榮＋補寄運費 | 原始資料沒有此欄，是賣家額外實付物流費 | **無法自動算**，工具需提供「逐筆訂單」手動輸入框，預設 0 |
| 廣告扣款 | 蝦皮廣告後台查得的金額，範例中為寫死數字 | **無法自動算**，工具保留一個手動輸入欄位（單一數字，非逐筆），預設 0 |
| 期間蝦皮進帳 | 訂單金額 − 成交手續費 − 其他服務費 − 金流處理費 − 訂單退貨 − 大榮運費 − 發票費用 − 廣告扣款 | 自動算（依上列各項）|

### 3. 品項出貨數量與銷售金額

- 依「商品選項貨號」分組，統計當期出貨數量（數量加總）與銷售金額（總價加總）。**分組鍵與表格/圖表上顯示的品項名稱都用「商品選項貨號」**（若貨號欄位為空才退而用「商品選項名稱」補位），不使用「商品選項名稱」當顯示名稱。
- **不維護品項主檔**：售價採當期實際成交價，只列出當期實際出現過的品項；不需列出本期未出貨的品項（與人工範例的作法不同，範例會列所有固定品項含0，本工具簡化為僅列本期實際出現者）。

### 4. 排行圖表

- 從當期出現的品項中，各自依「銷售數量」與「銷售金額」排序取前 10 名。
- 產出兩張圖：數量排行圖、金額排行圖。

## 已知需手動輸入的資料（無法從原始資料取得）

1. **大榮＋補寄運費**：逐筆訂單金額，工具需提供輸入介面。
2. **廣告扣款**：單一期間總額，工具需提供輸入欄位。

這兩項目前無範例資料來源可整合，維持手動輸入。未來若使用者能提供大榮對帳單或蝦皮廣告後台匯出格式，可再擴充自動抓取（目前不做，僅預留欄位）。

## 待確認/未來可能擴充方向

- 若日後「不成立原因」出現新的文字類型，需要重新跟使用者確認是否納入保留例外。
- 「退貨數量」欄位（原始資料第 35 欄）目前範例未使用，暫不處理，若使用者提出退貨相關報表需求再另行討論。
- 大榮運費、廣告扣款若未來有可匯入的外部資料格式，需重新設計匯入介面。
